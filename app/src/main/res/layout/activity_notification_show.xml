<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".Design.Ui.Activities.Notification.NotificationShowActivity">


    <TextView
        android:id="@+id/custom_notification_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Notification Title"
        android:textSize="20sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/custom_notification_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_below="@id/custom_notification_title"
        android:layout_marginTop="8dp"
        android:contentDescription="@string/todo"
        android:scaleType="centerCrop"
        android:src="@drawable/post" />

    <TextView
        android:id="@+id/custom_notification_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/custom_notification_image"
        android:layout_marginTop="8dp"
        android:text="Notification Message"
        android:textSize="16sp" />

</RelativeLayout>